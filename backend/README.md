# Backend

Placeholder for the future Solo backend (NestJS + PostgreSQL + Prisma).

## Status

- **Out of scope for Phase 0 frontend work.**
- No real server authorization, database, payment processor, storage service, or WebSocket server lives here yet.
- Frontend development uses typed contracts and MSW mocks inside `/frontend` until the backend phase begins.

## Intended future layout (not implemented yet)

```
backend/
  src/
    modules/
    common/
    config/
    database/
```

Do not add production backend logic until an approved backend-phase task explicitly authorizes it.
