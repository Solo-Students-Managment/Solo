# Documentation — Student Management System

This `docs/` folder contains product requirements, architecture notes, marketing collateral, and operational guides in both English and Persian. Each subfolder includes `en/` and `fa/` variants for English and Persian documents.

Structure: see folders: `PRD/`, `marketing/`, `backend/`, `frontend/`, `devops/`, `design/`, `pitch/` and the combined `PRD-Complete` files.
