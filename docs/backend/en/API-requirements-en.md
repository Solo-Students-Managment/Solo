# API Requirements (English)

## Authentication

- Token-based authentication (JWT or similar)

## Endpoints (examples)

- `POST /auth/login`
- `GET /students/:id`
- `POST /sessions`
- `GET /reports/attendance`

## Error handling

- Standardized error response with codes and messages
