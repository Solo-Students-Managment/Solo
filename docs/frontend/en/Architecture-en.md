# Frontend Architecture (English)

Overview of the feature-sliced architecture, provider composition, routing strategy, and state management approach.

- App shell: `app/` contains providers and router
- Features: `features/*` contain pages and feature components
- Components: shared primitives in `components/ui`

Routing: React Router with role-based protected routes.
